{
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "autoloader-suffix": "GlpiPluginActivity"
    },
    "require-dev": {
        "glpi-project/tools": "^0.8"
    },
    "require": {
        "setasign/fpdf": "^1.9"
    },
    "conflict": {
        "symfony/console": "<6.0"
    }
}
